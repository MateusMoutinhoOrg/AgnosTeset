# Generated by `agnos build` — do not edit by hand.
# `help` is a command like any other (entries.yaml + a generated new.go +
# handler.go); the only difference is that agnos writes all three, instead of
# the user writing entries.yaml and handler.go.
identifiers: ["help", "--help"]
category: Info
help: Display help for a command
long-description: |
  When called without arguments, lists every available command
  grouped by category. When called with a command name, shows
  detailed usage, arguments, flags, and examples for that command.
examples:
  - "help"
  - "help start"
args:
  - name: command
    type: string
    description: The command to describe; omit it to list every command
    examples:
      - "help start"
