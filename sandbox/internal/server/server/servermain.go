package server

import (
	"github.com/MateusMoutinhoOrg/AgnosTeset/sandbox/api"
)

func ServerMain(sandbox *api.Sandbox, props api.ServeProps) error {
	//needs to start the server and handle sandbox.routes based on if is Actionable and priority levels .
	return nil
}
